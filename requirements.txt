# Runtime dependencies only -- this is what ships in the container (Dockerfile).
# Test/lint tooling lives in requirements-dev.txt, kept out of the image on purpose.
functions-framework==3.*
flask==3.*
google-auth==2.*
requests==2.*
google-api-python-client==2.*
google-cloud-firestore==2.*
